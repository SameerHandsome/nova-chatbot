apiVersion: v1
kind: ConfigMap
metadata:
  name: nova-config
  labels:
    app: nova-chatbot
data:
  APP_ENV:             "production"
  PORT:                "8000"
  LANGCHAIN_PROJECT:   "nova-multimodal-chatbot"
  FRONTEND_URL:        "http://localhost:8000"